# 九键输入法开发全记录(详细版)

> 这是一份"讲故事"式的开发文档:按时间顺序记录我在小狼毫(weasel)输入法上
> 做出一个九键(T9)输入法的完整过程 —— 每一步想做什么、为什么这么想、
> 实际怎么做的、中间踩了哪些坑、怎么排查出来的。
>
> 写法上尽量照顾没有输入法开发经验的读者:先讲原理再讲操作,
> 出现的专业名词都会在第一次出现时解释。
>
> 项目位置:`d:\desktop\input\weasel`(fork 自 https://github.com/rime/weasel)
> 最终交付:桌面上的「九键输入法安装包.exe」

---

# 第一部分:从需求到原理

## 一、起点:用户想在电脑上用九键打字

### 需求从哪来

用户习惯手机上的九键输入法:每个键上有三四个字母,按几个数字就能打出词。
比如打"我们":wo 在 9 键和 6 键上,men 在 6、3、6 键上,连按 `96636`
候选里就出现"我们"。但 Windows 上几乎找不到好用的九键输入法。
于是用户提出了需求,由我(AI 助手)负责调研、设计和实现全部代码。

### 需求拆解

原始需求一句话:"我要在电脑上用九键打字。"我把它拆成三条可验收的目标:

1. 用电脑的**数字小键盘**当九键 —— 它正好有 0~9 十个键,物理上和手机
   九键最像,用户不用学新键位;
2. 按键能**自己改** —— 不一定非用小键盘,比如用户想用 w 代表 abc 也得行
   (这条是后来明确追加的,原话:"目前8对应abc,我可以修改成w对应abc");
3. 最后要做出一个**安装包**,拿到任何电脑上双击安装就能用
   (不是只能在我这台开发机上跑)。

第 3 条很关键:它决定了所有功能必须**编译进程序本身**,
不能依赖"在这台机器上手工改过的配置文件"。这个原则在后面救了两次命
(见第 10、11 章)。

### 选什么底座:为什么是 RIME + 小狼毫

我没有从零写输入法 —— 一个能用的输入法要处理键盘接管、候选窗口、
词库检索、智能组词、用户词学习……从零做是造轮子造到绝望的活。
正确姿势是找一个成熟的开源框架,在它上面加功能。选型如下:

- **RIME(中州韵算法)**:输入法"引擎",负责真正的智力工作 ——
  拿到编码、查词典、算候选词、学习用户词。它最特别的一点是:
  **输入方案是配置文件(YAML 文本),不是代码**。拼音、五笔、仓颉、
  双拼……全是几个文本文件定义的。这意味着我大概率不用碰 C++ 代码
  就能做出一个新方案 —— 对九键这种"已有原理的变体"来说是完美底座
  (后来确实如此:九键主体功能零 C++;但预览拼音、设置界面这类
  增强还是分别动了引擎和前端代码,见后文);
- **小狼毫(weasel)**:RIME 的 Windows 版"外壳"(前端),负责
  托盘图标、候选窗口绘制、接管键盘、和 Windows 系统打交道这些事。
  它是开源 C++ 项目,需要时也能改代码。

分工一句话:**RIME 管"想"(算什么候选),weasel 管"做"(收按键、画窗口)**。

### 核心原理:九键的本质是"字母翻译成数字"

想明白这一点,后面所有设计都顺了。普通拼音输入法打"我们":
敲 `women` 五个字母。九键打"我们":手机键盘上每个数字键挂着几个字母 ——

```
w 在 9(wxyz)上,o 在 6(mno)上      → wo 按 96
m 在 6(mno)上,e 在 3(def)上,n 在 6(mno)上 → men 按 636
合起来按:96636
```

所以九键输入法 = **字母到数字的翻译 + 一本"数字编码词典"**。
输入法引擎只要支持"把你按的键翻译成别的编码再去查词典",九键就是
换个翻译表的事。RIME 恰好原生支持这种玩法(双拼输入法就是同一机制,
双拼是字母映字母,我做的是字母映数字)。

九键键位表(和手机完全一致,这是行业标准 T9 布局):

| 键 | 字母 | 键 | 字母 |
|---|---|---|---|
| 2 | a b c | 6 | m n o |
| 3 | d e f | 7 | p q r s |
| 4 | g h i | 8 | t u v(含 ü) |
| 5 | j k l | 9 | w x y z |

另外两个约定:`1` = 逗号(手机上 1 键也是标点),`0` = 空格。

## 二、补课:RIME 输入法的零件有哪些

动手前我先花功夫搞懂了 RIME 的几个核心概念,否则后面的配置看不懂。
这里给同样零基础的读者解释:

### 2.1 方案(schema)—— 输入法的"说明书"

一个 `xxx.schema.yaml` 文件定义一种输入法的完整行为:接受哪些按键、
编码怎么拼、查哪本词典、标点怎么处理、哪个键翻页……
比如"明月拼音"是 `luna_pinyin.schema.yaml`,"五笔"是另一个文件。
我要做的九键,就是新写一个 `t9.schema.yaml`。

### 2.2 词典(dict)—— 输入法的"词本"

`xxx.dict.yaml`,一行一条:`词  编码  词频`,例如:

```yaml
我们	wo men	20000
你好	ni hao	18000
```

词典决定"能打出来什么词"。词频决定候选排序(常用的排前面)。

### 2.3 棱镜(prism)—— 编译出来的"查找索引"

这是 RIME 里最绕但最巧妙的概念。词典里的编码(比如拼音 `women`)
要变成引擎能快速查找的结构,这个编译产物叫棱镜(一个 .bin 二进制文件)。
**编译时可以加一层"拼写代数"(speller/algebra)规则,把编码变形** ——
比如把 `women` 按字母表映射成 `96636` 再存进棱镜。
这样:词典里存的是拼音,棱镜里存的是数字,用户按数字就能查到词。
"部署"这个词后面经常出现,意思就是"把 YAML 文本编译成 .bin 索引"。

### 2.4 补丁(custom.yaml)—— 不改原文件的"覆盖层"

RIME 支持给任何方案打补丁:建一个 `t9.custom.yaml`,里面写
`patch:` 开头的片段,部署时会合并进原方案。**用户的自定义按键就是
靠这个机制实现的** —— 永远不修改方案原文件,只叠加补丁,
可以随时删掉补丁恢复原样。

### 2.5 按键绑定(key_binder)—— 按键的"翻译官"

方案里可以写一张表:"收到 A 键时,当作 B 键处理"。例如:

```yaml
key_binder:
  bindings:
    - {accept: KP_2, send: '2', when: always}
```

读法:accept = 收到什么键(KP_2 是小键盘 2 的键名),
send = 改发什么键('2' 是数字 2),when = 什么时候生效。
**when 有两种取值,这个细节酿成过本项目最大的一个用户可见 bug**
(详见第 10 章):

- `when: always` —— 任何时候都翻译;
- `when: composing` —— 只在"已经开始输入编码"之后才翻译。

### 2.6 用户目录 vs 程序目录

- **程序目录**(`C:\Program Files\Rime\weasel-0.17.4\`):方案和词典
  原文件,安装包释放到这里,只读;
- **用户目录**(`C:\Users\xxx\AppData\Roaming\Rime\`):补丁文件、
  用户词库、部署产物都在这里。**自定义按键写入的就是这里的
  `t9.custom.yaml`**。

理解这套零件之后,开发路线就清楚了:
**写词典 → 写方案 → 编译打包 →(后来追加)改引擎显示拼音 →
(再后来)写图形设置界面**。

---

# 第二部分:方案开发

## 三、第一步:造词典

### 我的想法

输入法要打得出词,必须有词典。手工录入几十万个词不现实 —— 好在现成的
开源词库有两个:

- **rime-luna-pinyin**(明月拼音词典):每条带"词 + 拼音 + 词频",
  质量高,约 8 万条;
- **rime-essay**(常用词表):约 23 万条,按使用频率统计出来的,
  但只有"词 + 词频"两列,**没有拼音**。

两个合并,覆盖面和常用度都够了。

### 词典生成脚本怎么工作

我写了 `weasel/tools/gen_t9_dict.py`,流程四步:

1. **读 luna-pinyin**:格式现成,直接拿到"词 + 拼音";
2. **读 essay 并补拼音**:缺拼音?用 Python 的 `pypinyin` 库
   自动给每个词标注(比如"我们"→ wo men)。命令行加 `--essay` 参数;
3. **合并排序去重**:两边合到一起,按词频从高到低排序;
   "词 + 编码"都相同的重复条目去掉;
4. **输出 RIME 词典格式**:每行 `词\t编码\t词频`,加上 YAML 头部声明。

用法:

```
python tools/gen_t9_dict.py --luna <luna词典路径> --essay <essay.txt路径> --out data/t9.dict.yaml
```

最终产出 `weasel/data/t9.dict.yaml`,**478,842 条**。

### 两个没料到的坑

**坑 1:essay 是繁体词库。** 打开一看全是"我們""這個"。
第一反应是写脚本转简体,但想了想否决了:转完就丢失繁体能力了。
更好的解法是**在方案里挂一个 OpenCC 繁简转换插件**(`simplifier`),
词典原样保留繁体,输出时默认转简体,想要繁体随时能切回去。
零转换成本,还白赚繁体支持。

**坑 2:essay 新版格式变了。** 老版本 essay 是"词+拼音+词频"三列,
新版只有"词+词频"两列。脚本要兼容两种:检测到只有两列就自动
用 pypinyin 补拼音。

### 一个后来证明很关键的决定:词典存拼音还是存数字?

词典编码有两种存法:

- **存数字**:`我们  96636  20000` —— 直白,但编码是"死"的,
  引擎拿到用户输入只能拿数字对数字;
- **存拼音**:`我们  wo men  20000` —— 编码是"活"的,
  数字映射放到棱镜编译阶段用代数规则做(第 2.3 节讲过)。

我选了后者。当时的理由只是直觉:拼音更"原始",数字是派生的,
存原始形式以后想做什么都方便。这个决定当时没明确的好处,
直到用户提出"预览栏显示拼音"的需求(第 7 章),它才应验:
**只有词典存拼音,引擎才有能力把数字编码翻译回拼音显示**。
如果当初存的是数字,这个需求就得推翻词典重做。

## 四、第二步:写方案文件

### 方案文件的骨架

方案文件(schema)是告诉 RIME"这个输入法怎么运作"的配置。
`weasel/data/t9.schema.yaml` 要声明这么几件事,逐个讲:

### 4.1 只认识数字(speller)

```yaml
speller:
  alphabet: '23456789'
  initials: '23456789'
```

声明:本方案只接受数字 2~9 作为编码。按字母键引擎不理
(除非被 key_binder 翻译过)—— 这正是九键要的。

### 4.2 拼写代数:字母→数字的翻译表

```yaml
speller:
  algebra:
    - xlit/abcdefghijklmnopqrstuvwxyz/22233344455566677778889999/
```

这行是本方案的核心魔法,值得拆开讲:

- `xlit/A/B/` 是 RIME 的"逐字符替换"指令,像 sed 的 `y` 命令;
- 第一个串是 26 个字母,第二个串是 26 个数字,一一对应:
  a→2、b→2、c→2、d→3、e→3、f→3……(对照第 1 章键位表);
- 数字串里 7 和 9 各有 4 个字母(pqrs、wxyz),所以 `7777`、`9999`。

效果:部署(编译棱镜)时,词典里的 `wo men` 被自动映射成 `96 636`,
用户按小键盘数字就能查到词。**词典本身一个字节都没改**。

### 4.3 查词典(translator)

```yaml
translator:
  dictionary: t9        # 用哪本词典
  prism: t9             # 用哪个棱镜索引
  enable_completion: true   # 逐键补全:按到一半就给候选
  enable_sentence: true     # 整句模式:支持多词连打
```

`enable_completion` 是体验关键:按了 `96` 就把所有以 96 开头的词
都列出来,手机九键就是这种感觉。

### 4.4 繁转简(simplifier)

```yaml
zh_variant:
  option_name: zh_simp
  tips: all
  opencc_config: t2s.json   # 繁→简
engine:
  filters:
    - simplifier@zh_variant
```

挂上这个插件,词典里的"我們"输出时自动变"我们"。
(3.2 节说过,这是为了不动繁体词库)

### 4.5 标点(punctuator)

```yaml
punctuator:
  '1': '，'    # 按 1 直接上屏逗号
  '0': ' '    # 按 0 上屏空格
  '.': '。'
```

手机上 1 键是标点、0 键是空格,这里保持一致。
(已知限制:1 键目前只出逗号,没有标点候选列表,需要可自行改方案)

### 4.6 按键绑定(key_binder)—— 小键盘进场

```yaml
key_binder:
  bindings:
    # 小键盘数字 → 编码数字
    - {accept: KP_0, send: '0', when: always}
    - {accept: KP_1, send: '1', when: always}
    - {accept: KP_2, send: '2', when: always}
    ... (KP_3 ~ KP_9 同理)
    # 小键盘其他键
    - {accept: KP_Subtract, send: Page_Up, when: composing}    # - 翻页
    - {accept: KP_Add, send: Page_Down, when: composing}       # + 翻页
    - {accept: KP_Decimal, send: period, when: composing}      # . 句号
```

`KP_*` 是 RIME 的小键盘键名(KP = Keypad)。
这里有个"白捡"的便利:我本来担心 Windows 的小键盘按键能不能被
RIME 识别,去翻 weasel 源码 `WeaselTSF/KeyEvent.cpp`,发现它原生
就把 Windows 的 `VK_NUMPAD0~9` 虚拟键码翻译成 `KP_0~KP_9` ——
**一行代码都不用写,小键盘天然可用**。这就是选成熟底座的好处。

`when: always` 让数字键随时可起头输入;翻页之类用 `when: composing`
(只在打字中生效,平时按 +/- 不会捣乱)。

### 4.7 其他细节

- `menu/page_size: 5`:候选窗一页显示 5 个(手机九键候选不贪多);
- 方案文件头部写了大段注释,把用法和自定义示例写在文件里,
  高级用户打开文件就能看懂。

### 4.8 "位置对应"方案:用户的第二个要求

用户试用后反馈了一个别扭的事:手机九键是 **123 在上、789 在下**,
电脑小键盘是 **789 在上、123 在下** —— 上下颠倒。
按手机肌肉记忆去按小键盘,位置全错。

解法:做第二个方案 `t9_pos.schema.yaml`(位置对应),不按数字含义,
按**物理位置**对应:

| 你按小键盘 | 7 | 8 | 9 | 4 | 5 | 6 | 1 | 2 | 3 | 0 |
|---|---|---|---|---|---|---|---|---|---|---|
| 等于手机键 | 1标点 | 2abc | 3def | 4ghi | 5jkl | 6mno | 7pqrs | 8tuv | 9wxyz | 0空格 |

实现上偷了个懒但偷得很正当:两个方案的差别**只有 key_binder 映射表**,
词典、棱镜、拼写规则完全一样。所以 t9_pos 里直接写:

```yaml
translator:
  dictionary: t9    # 共用 t9 的词典
  prism: t9         # 共用 t9 的棱镜
```

零额外词典开销,编译也快。两个方案可以用 `Ctrl+Shift+1` 来回切换
(RIME 内置的方案切换快捷键)。

### 4.9 把九键设为默认方案

安装后用户看到的方案列表由 `default.yaml` 决定。我从官方模板
(rime-prelude)复制一份,把 t9 和 t9_pos 插到列表最前面:

```yaml
schema_list:
  - schema: t9          # ← 新增
  - schema: t9_pos      # ← 新增
  - schema: luna_pinyin
  ...
```

顺手删掉了官方发布里不含的 quick5(速成五代),避免部署报错。

### 4.10 让用户能手工自定义:模板文件

图形界面是后来才做的,最初自定义按键靠手工编辑文件。
我准备了模板 `data/t9.custom.yaml`,里面是空补丁 + 大段注释示例,
包括一个"用笔记本主键盘字母模拟九键"的完整例子。
用户复制到用户目录改一改、重新部署即可。
(这部分后来被图形界面取代,但模板保留,高级用户仍可用)

### 4.11 构建管线:让安装包带上这些文件

weasel 的官方构建脚本 `build.bat` 只打包标准方案。我在它的
`:build_data` 阶段追加了一行 `xcopy`,把 `weasel/data/*`
(九键三件套 + default.yaml)覆盖进 `output/data`,
这样打出来的安装包里就有九键了。

---

# 第三部分:编译打包

## 五、第三步:从零编译出安装包

### 为什么必须自己编译

方案文件只是文本,理论上可以手动塞进官方安装包里。但:

1. 我要的是"拿到任何电脑都能装"的完整安装包;
2. 后面还要改 C++ 代码(预览栏拼音、设置界面),改代码必须重新编译。

所以绕不开:**从源码把 weasel 整个编译一遍**。
这也是用户明确要求的("全源码编译")。

### 编译链有多长

weasel 不是一个小工程,编译它要先编译一串依赖:

```
Boost(C++ 基础库,约10个子库)
  → librime(RIME 引擎,又内嵌 glog/leveldb/yaml-cpp/
              googletest/marisa-trie/opencc 六个子模块)
    → weasel 本体(WeaselServer / WeaselTSF / WeaselUI / WeaselDeployer)
      → NSIS 打安装包
```

### 环境与工具

| 项目 | 版本/说明 |
|---|---|
| Visual Studio | VS18 Community(MSVC 14.51,工具集 v145) |
| Windows SDK | 10.0.26100.0 |
| Boost | 1.84.0(源码下载解压,现编) |
| NSIS | 3.10(打安装包) |
| Git Bash | plum 数据安装脚本需要 |
| 架构 | x64 + Win32 双份(安装包要同时支持 32/64 位系统) |

所有环境变量集中在 `weasel/env.bat`(BOOST_ROOT、BJAM_TOOLSET、
PLATFORM_TOOLSET、DEVTOOLS_PATH 等),换机器只要改这一个文件。

### 构建流程(固化成四个脚本)

| 阶段 | 脚本 | 干什么 |
|---|---|---|
| 准备 | git submodule | 拉取 librime 和它的六个子模块 |
| Boost | stage_boost.bat | b2 编译 x86+x64 静态库 |
| librime | stage_rime.bat | cmake + msbuild 编出 rime.dll |
| weasel | stage_weasel.bat | plum 装标准方案 + msbuild 编 weasel.sln |
| 安装包 | stage_installer.bat | makensis 打包 install.nsi |

以后改一行代码,重跑后两个脚本即可(增量编译)。

### 踩坑记录:新编译器 + 老依赖的步步血泪

环境是"最新的 VS"配"几年前的依赖库",于是每个环节都在打架。
按时间顺序记录(每条都是真实卡住过的):

| # | 现象 | 原因 | 解法 |
|---|---|---|---|
| 1 | Boost 编译第一步报 "Unknown toolset: vcunk" | Boost 的工具链识别表里没有 VS18,生成了个叫 vcunk 的鬼名字 | 写补丁脚本 `guess_toolset.bat`:识别不出的新 VS 一律按兼容模式(vc143)处理 |
| 2 | Boost 构建脚本不认识新版 MSVC | msvc.jam 里已知版本只写到 14.3 | 补丁 msvc.jam:版本匹配放宽为"14.x 全收",路径检测补 14.4/14.5 分支 |
| 3 | Boost 编完了,链接却说找不到库 | Boost 头文件的"自动链接"逻辑按编译器版本算出库名 vc143,但 b2 实际生成的是 vc145 | 把 44 个库文件批量改名 vc145→vc143,同步替换 stage/lib/cmake 里的配置 |
| 4 | env.bat 里 BOOST_ROOT 时灵时不灵 | 用了 `%CD%` 相对路径,在子目录调 msbuild 时就指错了 | 改绝对路径 |
| 5 | 编译资源文件报 afxres.h 缺失 | VS 默认不装 MFC/ATL 组件 | VS Installer 静默加装 ATLMFC(需要管理员权限) |
| 6 | NSIS 打包报宏未定义 | 安装脚本用了 `${AtLeastWin11}`,旧版 NSIS 3.08 没这个宏 | 升级 NSIS 到 3.10 |
| 7 | CMake 直接拒绝配置 librime | librime 的 CMakeLists 声明的最低版本太老,新版 CMake 4 不伺候 | 设环境变量 `CMAKE_POLICY_VERSION_MINIMUM=3.5` 放宽 |
| 8 | 数据安装脚本跑起来是 WSL 的 bash | 系统 PATH 里 WSL 排在 Git 前面 | DEVTOOLS_PATH 把 Git 路径前置 |
| 9 | 测试脚本加载 rime.dll 报错 | 64 位 Python 加载不了 32 位 dll | 下载 32 位嵌入式 Python 专用于测试(后来又改用 64 位引擎+64 位 Python) |

经验:依赖库不认识新编译器是常态,补丁思路无非两种 ——
要么让它"装作认识"(兼容模式),要么让产物"对上暗号"(改名)。

### 构建产物

- `output\rime.dll` + `output\Win32\rime.dll` — 新编译的引擎(64/32 位);
- `output\WeaselServer.exe`、`WeaselTSF.dll`、`WeaselUI.dll`、
  `WeaselDeployer.exe` 等全套程序(双架构);
- `output\data\` — 标准方案 + 九键三件套 + OpenCC 数据;
- `output\archives\weasel-0.17.4.0.f9203ca-installer.exe` —
  **最终安装包(约 15 MB)**。

有个小细节:安装包里的 exe 取自 `output\Win32\`,和 `output\` 根目录
的同名 exe 是两份(分别给 32/64 位系统用),哈希不同但内容等价。
验证安装包内容时要对着 Win32 那份核对 —— 我最初对着根目录哈希
比对不上,虚惊一场。

---

# 第四部分:功能增强

## 六、第四步:让预览栏显示拼音

### 需求

打字时候选窗上方有一行"预览",显示你正在输入的内容。
拼音输入法显示 `women`,很直观;九键下显示的是 `96636` 这种数字串 ——
完全看不懂自己在打什么,打错了都不知道错在哪。
用户希望它显示 `wo'men`。

### 为什么这事比看上去难

第一反应:让引擎"把数字翻译回拼音"不就行了?
但引擎预览栏的默认逻辑是"**把你按的键原样显示**" —— 你按的是数字,
它就显示数字,引擎从头到尾不知道这些数字"曾经"是拼音。
数字→拼音的映射在棱镜编译时就"单向烧掉"了(4.2 节)。

要让预览显示拼音,引擎必须具备**反向解码**能力:
拿着数字编码,去词典里查它对应哪个拼音音节。
这就回到第 3.4 节那个决定:**词典存的是拼音**,反向解码在数据上可行 ——
缺的只是引擎里的一个开关。

### 实现:三块拼图

**拼图 1:词典存拼音**(第三章已做好,不用动)。

**拼图 2:方案里的 xlit 映射**(4.2 节已写好,不用动)。
它保证棱镜里存的是数字索引,用户按数字能查到拼音词条。
这和双拼输入法的原理一模一样:双拼是"字母映字母",我是"字母映数字",
引擎眼里两者是同一种操作。

**拼图 3:给引擎加开关**(本项目唯一一次动**引擎** C++ 代码)。
改的是 `librime/src/rime/gear/script_translator.{h,cc}` 这两个文件。
下面把修改过程拆成五步,每步都对应真实代码。

**第 1 步:先找到预览内容是在哪生成的。**
引擎里负责"拼音类"方案的翻译器叫 `ScriptTranslator`,它生成候选时
会给每个候选设置一个 preedit(预览文本)。定位到
`script_translator.cc` 的 `ScriptTranslation::Peek()` 函数里有这么一段:

```cpp
if (candidate_->preedit().empty()) {
  candidate_->set_preedit(
      syllabifier_->GetPreeditString(*candidate_));  // ← 预览就是这里填的
}
```

`GetPreeditString` 取的是"用户输入串经过音节切分后的样子" ——
用户按的是数字,它切出来的自然还是数字。要改成拼音,
就在这一个位置做文章。

**第 2 步:给类加一个开关成员。** 在 `script_translator.h` 的
`ScriptTranslator` 类里,仿照旁边已有的选项(spelling_hints 等)
加一对:

```cpp
bool preedit_uses_spelling() const { return preedit_uses_spelling_; }
// ...
bool preedit_uses_spelling_ = false;   // 默认关,不影响任何现有方案
```

**第 3 步:让开关能从方案配置里读。** 在构造函数里,
紧挨着其他选项的读取语句加一行:

```cpp
config->GetBool(name_space_ + "/preedit_uses_spelling",
                &preedit_uses_spelling_);
```

这样方案 yaml 里写 `translator/preedit_uses_spelling: true`
就能传进来了。RIME 的选项机制就是这么工作的:
配置键名 + 成员变量 + 构造函数读取,三件套。

**第 4 步:改第 1 步找到的那段代码,加分支。**
开关打开时,不再取输入串,而是**用词典把编码解码回音节**:

```cpp
if (candidate_->preedit().empty()) {
  if (translator_->preedit_uses_spelling()) {
    // 显示解码出的拼写(拼音)而不是原始输入码(九键数字)
    string spelling;
    vector<string> syllables;
    if (auto* dict = translator_->dict()) {
      if (dict->Decode(candidate_->code(), &syllables) &&
          !syllables.empty()) {
        spelling = boost::algorithm::join(
            syllables, string(1, translator_->delimiters().at(0)));
      }
    }
    candidate_->set_preedit(translator_->FormatPreedit(spelling));
  } else {
    candidate_->set_preedit(syllabifier_->GetPreeditString(*candidate_));
  }
}
```

关键在 `dict->Decode(...)`:候选自带它命中的编码(code),
词典能把编码反向解码成音节列表(如 `wo`、`men`),
再用分隔符(拼音方案里是 `'`)拼起来就是 `wo'men`。
这一步能成立,前提是第 3.4 节那个决定 —— 词典存的是拼音,
所以"解码"才有拼音可给;如果词典存的是数字,
这里解出来还是数字,整个思路就不通了。

**第 5 步:方案里启用,重编译验证。**
两个九键方案的 translator 段各加一行:

```yaml
translator:
  preedit_uses_spelling: true
```

然后重跑 stage_rime.bat 编出新的 rime.dll(x64 + Win32 两份),
跑 test_t9.py:按 `96636`,预览栏从 `96636` 变成 `wo'men`,
候选仍是"我们" —— 13/13 全过。同时确认官方标准方案
(明月拼音等)不开这个选项时行为一点没变,
这就是"默认关 + 按方案启用"控制风险的方式。

## 七、第五步:托盘菜单入口

### 需求

自定义按键总得有个入口。用户希望放在任务栏托盘图标的右键菜单里:
右击"中"字图标 →「九键按键设置」。这是最符合直觉的位置,
用户不用找任何窗口。

### 实现链路(看起来三步)

1. **加菜单项**:托盘菜单定义在资源文件(.rc)里,给菜单插入一项,
   分配一个新 ID `ID_WEASELTRAY_T9KEYS`;
2. **加处理函数**:用户点这个菜单项时,`WeaselServerApp.cpp` 里
   启动 `WeaselDeployer.exe /t9keys`(配置工具带参数启动,
   直接弹出九键设置对话框);
3. **加命令行入口**:WeaselDeployer 的程序入口识别 `/t9keys` 参数,
   分派到设置对话框。

### 大坑:菜单有两份

做完上面三步,编译、在本机跑 —— 菜单出现,一切正常。
然后打了个新安装包装上,**菜单项不见了**。
反复确认安装包里的 WeaselServer.exe 确实是新编的,就是不生效。

这种"产物明明是对的,行为却是旧的"现象最折磨人。
排查到最后真相让人哭笑不得:weasel 的托盘菜单资源文件有**两份**:

- 一份在 `WeaselServer.rc`,编进主程序 WeaselServer.exe;
- 一份在 `WeaselTSF.rc`,编进 weasel.dll/weaselx64.dll ——
  这个 dll 是被系统的 explorer.exe 加载的。

### 为什么会有两份?—— 上游演进留下的双轨结构

这不是巧合。我翻了上游 rime/weasel 的 git 历史,时间线是这样的:

**第一代设计(2011 年):托盘在服务器进程里。**
`WeaselServer/SystemTraySDK.cpp` 最早出现于 2011 年 12 月的提交
"Weasel tray icon":WeaselServer.exe 自己调 `Shell_NotifyIcon` 创建
托盘图标,右键时从 exe 自己的资源加载菜单弹出 —— 菜单自然定义在
`WeaselServer.rc` 里。

**语言栏代码 2012 年就有了,但休眠了六年。**
`WeaselTSF/LanguageBar.cpp` 的第一次提交是 2012 年 8 月的
"Initial language bar bits",但一直没启用。

**第二代设计(2018 年启用):托盘改用 TSF 语言栏按钮。**
2018 年 4 月,上游合并 PR #170 "feat(tsf): enable language bar
button",语言栏按钮(`CLangBarItemButton`)正式上岗。看
`WeaselTSF/LanguageBar.cpp`:`LoadMenuW(g_hInst, ...)` ——
`g_hInst` 是 DLL 自己的实例句柄,也就是被 explorer.exe 加载的那份
weasel.dll。菜单资源因此必须在 `WeaselTSF.rc` 里。
同一批提交里还有一条 "show deploy notification even if tray icon is
disabled" —— 服务器侧托盘图标就是从那时起默认关闭的
(今天 `weasel.yaml` 里仍是 `display_tray_icon: false`,代码里
`WeaselTrayIcon::Create()` 见到 false 就 `RemoveIcon()`)。
至于当年为什么启用语言栏按钮,PR 里没写理由,不得而知。

**为什么旧的没删?** 服务器侧那套托盘代码一直保留着
(默认不出图标,但代码还在编译),0.17.4 基线把这份双轨结构
原样继承了下来。

### 再叠加一条 Windows 铁律,坑才真正成立

**菜单资源必须从"窗口宿主所在的那个模块"加载。** 图标窗口现在
寄宿在 explorer 进程里的 dll 中,`LoadMenu` 用的是 dll 的句柄 ——
WeaselServer.exe 里的菜单资源**物理上够不着**。所以只改 exe 那份
等于改了个没人读的文件。

而且 DLL 和服务器是两个进程:用户在 explorer 侧点菜单项,
DLL 自己执行不了"部署""打开设置"这类服务器功能,只能把命令 ID
通过管道 IPC 转发(`m_client.TrayCommand(wID)`)给服务器执行。
这就是为什么**两边都要有同一个 ID**:dll 侧负责"画出来并转发",
server 侧负责"接住并执行"。

一句话总结:**一次没清理干净的上游架构迁移 +
Windows"资源跟着窗口宿主走"的机制 +
双资源之间没有任何自动同步手段**,三者叠加,
就出现了"产物全对、行为是旧的"。

而当时的我**只改了前者,等于改错了文件** ——
explorer 加载的 dll 里根本没有新菜单项。

还有第二重坑:就算两份都改对了,改完 dll 也必须**重启电脑**
(至少重启 explorer)才能生效 —— explorer 把旧 dll 缓存住了。
"改了没生效"叠上"要重启才生效",两个因素互相掩护,
让这个问题显得格外灵异。

修复清单:

1. `WeaselTSF.rc` 与 `WeaselServer.rc` 两份菜单同步加项;
2. `include/resource.h` 与 `WeaselServer/resource.h` 两份头文件
   的 ID 定义保持一致;
3. 顺手补回"重启算法服务"菜单项(fork 基线源码里只有 ID 没有菜单项,
   但用户旧版小狼毫里有这一项,缺了会疑惑)。

**教训:凡是"资源文件",先全局搜索有几份。**

---

# 第五部分:设置界面 —— 本项目折腾最多的部分

## 八、第六步:设置界面的三轮重写

先交代范围:这一章的所有工作都是**weasel 前端 C++** ——
新写对话框 `T9KeySettingsDialog.{h,cpp}`(约 500 行),改
`WeaselDeployer.cpp`(命令行入口)、`Configurator.{h,cpp}`(保存后
自动部署)、`resource.h`/`WeaselDeployer.rc`(界面资源,三语言),
外加上一节讲的托盘菜单两份 rc。引擎(librime)一行没动 ——
自定义按键本质上还是写 key_binder 补丁,引擎早就支持。

用户对界面提了三轮要求,界面就重写了三轮。
这一章完整记录每一版的设计、失败原因和修复过程,
是全文最有"工程感"的部分。

### 8.1 第一版:列表式(被否)

**设计**:一个列表,每行显示一条绑定,形如
`accept: KP_2, send: 2, when: always`,下面是"添加/删除"按钮。
本质是把 custom.yaml 的每一行画出来。

**结果**:用户看了一眼:"看不懂这个界面是怎么用的。"

**反思**:这是典型的程序员视角 —— 我熟悉 yaml 语法,觉得列表很自然;
但用户的心智模型是手机九键的九宫格,不是配置文件。
第一版被否不是做得差,是方向错。

### 8.2 第二版:九宫格 + 按键捕获(方向对了,但不生效)

用户把需求说得很直白:"现在 8 对应 abc,我想改成 w 对应 abc;
9 对应 def,我想改成 e 对应 def。"

于是界面重做成手机九键的样子:

- **布局**:3×3 九宫格按钮 + 下方一个 0 键,共 10 个按钮;
  按钮 1 = 标点、按钮 2 = abc、……按钮 9 = wxyz、按钮 0 = 空格;
- **每个按钮两行字**:上行是组标签(如 "2 abc"),下行是当前绑定的键
  (默认显示"小键盘 2");
- **交互(按键捕获)**:点某个按钮 → 界面提示"请按下要绑定的按键"
  → 用户按 w → 按钮变成 `abc / w`。Esc 取消、Backspace 恢复该组默认;
- **冲突检查**:同一个键被绑到两组时弹框提示;
- **恢复默认按钮**:删掉该方案的 custom.yaml,全部还原;
- **确定**:保存并自动重新部署。

这个设计用户满意了,但测试时发现:**点完按钮按键盘毫无反应,
确定后提示"已部署"但也不生效**。排查出两个病根:

**病根 1:回调参数顺序写反(低级但致命)。**
Windows/ATL 框架里,一组按钮共用的回调宏 `COMMAND_RANGE_HANDLER`
的函数签名是:

```cpp
LRESULT OnKeyBtn(WORD wNotifyCode, WORD wID, HWND hWndCtl, BOOL& bHandled)
//               ↑通知码(第几个参数)   ↑控件 ID
```

我写成了 `(WORD wID, WORD, ...)` —— 把通知码当成了控件 ID。
程序拿 0(通知码)去减按钮基址 ID,算出负数,
判断"不是我的按钮",直接 return —— 一声不吭。
这种错编译器不报、运行不崩、日志没有,是最难查的一类。

**病根 2:键盘钩子在这台机器上失灵。**
捕获"用户按下的任意键"我最初用的是 Windows 全局低级键盘钩子
(`WH_KEYBOARD_LL`)。自动化测试显示:直接给对话框发消息能改按钮文字
(说明对话框处理正常),但注入按键钩子收不到 —— 钩子环节整个失效,
怀疑被安全软件拦截。替代方案:不用钩子,改用**轮询** ——
系统 API `GetAsyncKeyState(vk)` 能查到"某个键自上次查询后
有没有被按下过"(返回值的最低位),每 15 毫秒把常用虚拟键
都查一遍即可。进入轮询前先把全部 256 个键查一遍建立基线,
避免把之前按着的键误判为新按键。

修复后自动化验证通过:点按钮、按 w,按钮文字变 `abc / w`。

### 8.3 第三版:修复"卡死"(非阻塞重构)

用户装了新包反馈:"恢复默认、确定、取消、关闭,都会卡死。"

**原因**:第二版的捕获是**阻塞式**的 —— 点按钮后程序钻进一个
"死循环等按键"(while 循环 + Sleep 轮询),这个循环卡在按钮的
消息处理函数里,整个对话框的消息循环被它独占。
捕获期间用户点其他任何按钮,消息都排不进处理队列 ——
界面对一切点击无反应,看起来就是卡死。

**重构**:把捕获改成**定时器驱动**的非阻塞设计:

1. 点键位按钮 → 不循环,只是启动一个每 15 毫秒触发一次的
   系统定时器(`SetTimer`),然后函数立即返回;
2. 每次定时器触发时查一遍按键状态,查到目标键就完成绑定、
   停掉定时器;查不到就等下一次触发;
3. 对话框的消息循环全程正常运转 —— 捕获期间点取消能关窗、
   点别的键位能切换捕获目标、点恢复默认能弹确认框。

**顺带发现第三个病根:取消按钮根本没接上线。**
ATL 框架的对话框,一旦重写了消息表(BEGIN_MSG_MAP),
框架内置的"IDCANCEL → 关窗"默认行为就失效了。
也就是说之前几版的"取消"按钮**点了等于没点** ——
用户报的"取消不了"有一半功劳是它的。
显式补上 `COMMAND_ID_HANDLER(IDCANCEL, ...)` 后解决。

自动化测试五项全过:绑定生效、捕获中切换键位、捕获中取消关窗、
捕获中关闭窗口、捕获中恢复默认(弹确认框选"否"能回到界面)。

### 8.4 保存:点"确定"后写什么文件

设置保存到用户目录的 `t9.custom.yaml`(或 t9_pos.custom.yaml),
内容是 RIME 补丁格式:

```yaml
# T9KeySettings-v1 generated by WeaselDeployer (do not edit the marker line)
patch:
  key_binder/bindings/+:
    - {accept: w, send: '2', when: always}
```

这里有四个讲究,全是踩坑换来的:

1. **只写和默认不一样的绑定**。全默认就干脆删掉文件 ——
   文件越少越干净,也方便用户手工检查;
2. **首行打标记** `T9KeySettings-v1`。下次打开时靠它识别
   "这文件是我生成的":没有标记或有读不懂的内容,说明用户手工改过,
   保存前先弹框确认,不闷头覆盖(覆盖保护);
3. **`bindings/+` 这个语法**(血泪教训)。RIME 补丁里 `/+` 表示
   "往列表末尾追加";我一开始用了网上文档更常见的 `@next`,
   结果它把整个列表当成**一个元素**嵌套塞进去,
   引擎解析时**静默忽略** —— 不报错、不生效,又是最阴险的一类。
   这种"语法写错但没有任何报错"的问题,只能靠端到端测试兜底;
4. **`when: always` 而不是 `when: composing`** ——
   这是下一章的主角,本项目最大的一个用户可见 bug。

读取方向同样讲究:打开界面时用正则逐行解析现有 custom.yaml,
把绑定回填到九宫格上;解析不了的行不丢弃,原样记住,
保存时原样写回(不破坏用户手工内容)。

### 8.5 打开时默认选中用户正在用的方案

用户最后一个小要求:"我有两个九键方案,打开设置时应该默认选中
我正在用的那个。"

探测逻辑按顺序三级回退:

1. 读用户目录 `user.yaml` 的 `previously_selected_schema`
   (上次实际使用的方案),是九键方案就选它;
2. 不是九键,就读方案列表(default.custom.yaml 优先,
   其次程序目录的 default.yaml)里第一个九键方案;
3. 都没有就选第一项。

实现就是在配置文件文本里搜 `previously_selected_schema:` / `schema:`
这些键,取冒号后面的值比对 t9 / t9_pos。写完一测 —— 没生效,
又逮到一个 bug:

方案列表里写的是 flow 格式 `{schema: t9_pos}`,值后面紧跟 `}`。
我的比对逻辑是:先试"值完整等于 t9_pos" —— 取 7 个字符比较,
实际取到的是 `t9_pos}`(多了个右括号),永远不等;再试
"值开头是 t9" —— **t9 恰好是 t9_pos 的前缀**,误判命中,
永远返回 t9。修复:改成"前缀匹配 + 边界检查" ——
匹配到 t9_pos/t9 后,检查后一个字符不是字母数字下划线才算数;
顺手兼容引号包裹的写法 `{schema: "t9_pos"}`。

两种场景自动化验证:用户目录里上次方案是 t9 → 选中 t9;
上次方案不是九键、方案列表只有 t9_pos → 选中 t9_pos。都通过。

---

# 第六部分:两个"部署成功但不生效"的大坑

## 九、第七步:修复"修改后按键不生效"

### 现象

界面一切正常、保存提示成功、部署完成 —— 但按自定义键(如 w)打字毫无反应。
用户反馈:"界面现在没问题了,但是修改后按键不生效。"

### 排查过程

先看用户机器上实际生成的配置文件 —— 保存环节没问题,
`when: composing` 的绑定确实写进去了。那问题就在语义上。
回看第 2.5 节那个关键细节:

- `when: composing` = 只在"已经开始输入编码"之后才生效;
- `when: always` = 任何时候都生效。

我写的是前者。后果:必须先按一个数字键开了头,自定义键才有反应;
直接按 w,引擎根本不理 —— 用户当然是直接按 w 的,
体验上就是"完全坏了"。而方案里小键盘的默认绑定用的是
`when: always`,所以数字键能直接起头、字母键不能。
一行词的差别。

### 修复与验证

1. 把程序保存逻辑改成写 `when: always`(读取时两种写法都兼容);
2. **英文模式会不会误触?** 担心:always 意味着任何时候按 w 都会被
   翻译成数字 2,那英文模式下打 w 岂不变成 2?去翻引擎源码
   (librime 的 engine.cc):英文模式下按键在入口处
   (ascii_composer)就被拒绝(kRejected),后面的处理链
   (包括 key_binder)根本不执行 —— 不会误触;
3. 用户机器上的现有配置就地修正 + 重新部署,不用重装就能测;
4. 用和安装包同源的引擎跑了 7 项端到端测试全过:
   w 能直接起头、`wee` 和 `233` 候选完全一致、
   英文模式下 w 不被吞、原数字输入不受影响。

### 这个 bug 为什么能潜伏这么久

因为保存、部署、界面回显每个环节都"成功"了 ——
只有真正打字时才暴露。教训:**配置类功能的验证必须做到
"按键进去、字出来"的端到端,中间环节全绿不算数**。

## 十、第八步:安装验证的最后一坑

### 现象

设置界面的卡死修完、测试全绿、打包交付。用户装了,反馈:
"和上一版一模一样,还是卡死,还是取消不了。"

我这边自动化测试明明全过 —— 又是"产物是对的,行为是旧的"。

### 根因

去查用户机器上安装目录里的 WeaselDeployer.exe —— **时间戳还是旧的**。
文件根本没被换掉。原因:安装包双击安装会弹 UAC 提权确认
(装进 C:\Program Files 需要管理员权限),那次没点"是"
(或者旧的输入法服务进程还在运行,占着文件不放),
安装静默失败 —— 目录里还是旧文件。
装的"新版"其实是旧版,当然"问题一模一样"。

### 修复动作

1. 结束正在运行的输入法服务和残留的配置工具进程(释放文件占用);
2. 用管理员权限重新静默安装;
3. **核对安装目录**:exe 的时间戳、大小、特征字符串(新版才有的
   提示文案) —— 三项都对上才算装成功;
4. 重启输入法服务。

### 教训(现在是我的固定动作)

安装后不能只看"安装程序退出了",必须核对目标目录里的文件
确认真的被替换了;交付时明确提醒用户:UAC 弹框要点"是",
如果装完行为没变化,大概率是安装没真正执行。

---

# 第七部分:收尾

## 十一、测试:我信不过"看起来没问题"

本项目两次"部署成功但不生效"的坑都证明:中间环节全绿不等于功能可用。
所以我坚持**自动化端到端测试**:用 Python 直接加载编译好的 rime.dll,
模拟部署和按键,检查实际输出。不依赖人眼,改完代码随时重跑。

| 测试 | 验证内容 | 结果 |
|---|---|---|
| test_t9.py | 部署、`96636`→我们、预览拼音、逐键补全、标点、中英切换、简体输出 | 13/13 |
| test_t9_pos.py | 位置对应映射、标点/空格键位 | 6/6 |
| test_t9_custom_keys.py | 自定义按键补丁格式生效、与数字等效、未绑定键不干扰 | 9/9 |
| test_t9_custom_always.py | `when: always` 直接起头、英文模式不误触 | 7/7 |
| 界面自动化(临时脚本) | 捕获、冲突、捕获中取消/关闭/恢复/切换 | 5/5 |

界面测试的原理:用 PowerShell + Win32 API 启动配置工具、
找到对话框窗口、模拟点击和按键、读回按钮文字断言。
这里也踩过坑:`SendMessage` 模拟点击是同步的,如果按钮处理函数
里有阻塞循环,测试脚本自己会和被测程序死锁 —— 后来改成
后台线程点击。这类"测试工具本身的坑"也是测试工作的一部分。

## 十二、最终交付

- **安装包**:桌面「九键输入法安装包.exe」(约 15 MB,NSIS 打包,
  内含全部程序 + 方案 + 词典 + OpenCC 数据);
- **装完即用**:默认小键盘键位,不需要任何配置;
- **自定义**:托盘右键 →「九键按键设置」→ 九宫格界面,
  点键组按新键,确定即自动部署;
- **拿到新电脑**:安装包自带一切 —— 所有修复都在程序里,
  不依赖我这台开发机上的任何手工配置。这是第 1 章
  第 3 条需求(安装包可移植)的直接体现。

## 十三、复盘:这个项目教会我的事

1. **能用配置解决的就别写代码**。九键主体功能(打字本身)零 C++ 代码,
   全靠方案文件 + 词典 + 按键绑定 —— 只有体验增强(预览拼音、
   图形设置)才分别动了引擎和前端代码。改配置的风险和成本
   都比改代码低一个量级;
2. **"静默失败"是最贵的 bug**。参数写反不报错、`@next` 语法被忽略、
   `composing` 语义不符、安装失败不提示、t9 前缀误匹配 ——
   五个坑全是"没任何报错但就是不工作"。对策只有一条:
   任何改动后跑端到端测试;
3. **界面要按用户的心智模型设计**。"绑定列表"是程序员视角,
   "九宫格点哪按哪"才是用户视角;
4. **交互必须非阻塞**。任何"进入等待状态"的功能(比如等用户按键),
   都不能把主流程卡住,要用定时器/异步,否则旁边所有按钮都变摆设;
5. **交付验证要看终点**。安装包打对了 ≠ 用户机器上是新版。
   验证必须落到"目标目录里的文件确实换了";
6. **资源文件可能有同名多份**。托盘菜单的坑证明:
   全局搜一下再动手,比改完发现不生效便宜得多;
7. **存"原始形式"的数据更有生命力**。词典存拼音而不是数字,
   当时只是直觉,后来撑起了预览拼音功能。

---

# 附录

## 附录 A:文件清单

### 新增(weasel 仓库内)

```
weasel/data/t9.schema.yaml        九键方案(数字对应,核心)
weasel/data/t9_pos.schema.yaml    位置对应方案(小键盘位置→手机键)
weasel/data/t9.dict.yaml          拼音编码词典(478,842 条,脚本生成)
weasel/data/t9.custom.yaml        用户自定义按键模板(带注释示例)
weasel/data/t9_pos.custom.yaml    位置对应方案自定义模板
weasel/data/default.yaml          默认配置(t9、t9_pos 置顶)
weasel/data/key_bindings.yaml     依赖文件(来自 rime-prelude)
weasel/data/punctuation.yaml      依赖文件(来自 rime-prelude)
weasel/tools/gen_t9_dict.py       词典生成器
weasel/tools/test_t9.py           端到端测试(数字对应)
weasel/tools/test_t9_pos.py       端到端测试(位置对应)
weasel/tools/test_t9_custom_keys.py    自定义按键补丁测试
weasel/tools/test_t9_custom_always.py  when: always 语义测试
weasel/WeaselDeployer/T9KeySettingsDialog.h/.cpp  按键设置对话框
weasel/env.bat                    构建环境配置
```

### 修改

```
weasel/build.bat                     :build_data 追加 xcopy 九键文件
weasel/librime/src/rime/gear/script_translator.h/.cc
                                     新增 preedit_uses_spelling 选项
weasel/WeaselServer/resource.h / WeaselServer.rc / WeaselServerApp.cpp
                                     托盘菜单新增"九键按键设置"
weasel/include/resource.h / WeaselTSF/WeaselTSF.rc
                                     托盘菜单第二份资源(必须同步!)
weasel/WeaselDeployer/resource.h / WeaselDeployer.rc / Configurator.h/.cpp /
WeaselDeployer.cpp / WeaselDeployer.vcxproj
                                     /t9keys 入口与设置对话框
weasel/deps/boost_1_84_0/.../msvc.jam          补丁:支持新版 MSVC
weasel/deps/boost_1_84_0/.../guess_toolset.bat 补丁:新 VS 识别
```

### 构建辅助(d:\desktop\input)

```
stage_boost.bat / stage_rime.bat / stage_weasel.bat / stage_installer.bat
```

## 附录 B:名词对照表

| 术语 | 人话解释 |
|---|---|
| RIME / 中州韵 | 输入法引擎,管查词、出候选 |
| weasel / 小狼毫 | RIME 的 Windows 外壳,管收按键、画窗口 |
| 方案 schema | 一个 yaml 文件,定义一种输入法的规则 |
| 词典 dict | 词 + 编码 + 词频的大表 |
| 棱镜 prism | 词典编译出的二进制索引,查词靠它 |
| 部署 deploy | 把 yaml 文本编译成二进制索引的过程 |
| 补丁 custom.yaml | 不改原文件的覆盖层配置 |
| key_binder | 按键翻译器:按 A 当 B 使 |
| algebra / xlit | 编译棱镜时的逐字符替换规则 |
| KP_n | 小键盘数字键的键名(Keypad) |
| UAC | Windows 的管理员权限确认弹框 |
| TSF | Windows 的文字服务框架,输入法通过它接入系统 |

## 附录 C:使用说明(给最终用户)

1. 双击安装包安装(**UAC 弹框一定要点"是"**);
2. 系统输入法切到小狼毫,方案选"九键拼音"或"九键拼音·位置对应"
   (`Ctrl+Shift+1` 在两个方案间轮换);
3. 打开 Num Lock,小键盘直接打:2~9 = 字母,0 = 空格,1 = 逗号,
   `-`/`+` 翻页,空格或数字选候选,`Ctrl+Shift+2` 中英文切换;
   预览栏显示拼音(如 wo'men)而不是数字;
4. 自定义按键:右击托盘图标 →「九键按键设置」,点某个键组再按想用的键,
   确定即生效;Esc 取消捕获,Backspace 恢复单组默认,
   "恢复默认"清空全部自定义。

## 附录 D:已知限制

- 安装包只有 x86/x64,没有 ARM 版本;
- 词库基于 luna-pinyin + essay,不含用户自造词;
- `1` 键是单符号直接上屏(逗号),如需标点列表可自行改方案;
- 自定义按键的组合键(Ctrl/Alt/Win + 某键)不支持,只支持单键。
